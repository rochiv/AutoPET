import os
import time

import pandas as pd
import matplotlib.pyplot as plt

import nibabel as nib
import SimpleITK as sitk

# TODO: display segmentation over the PET image
# TODO: find the range (lower bound and upper bound) of pixel values for each image type (CT. CTres, PET, SEG, SUV)
# TODO: display all image types in one plot
